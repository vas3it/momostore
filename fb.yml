variables:
  VERSION: 1.0.${CI_PIPELINE_ID}

stages:
  - build-for-test
  - test 
##  - build-image-for-release
##  - deploy
##  - ver_f_downstream
##  - notify 

build-frontend-artefacts-for-test: 
  stage: build-for-test
  script:        
    - cd frontend
    - npm install
    - npm run build
  only:
    changes:
    - frontend/*  
  cache:
    key: frontend
    paths:
      - frontend/dist/frontend # для хранения фронта

sonarqube-frontend-sast:
  stage: test
  image: sonarsource/sonar-scanner-cli:latest
  script:
    - cd frontend
    - >
      sonar-scanner
      -Dsonar.sources=. 
      -Dsonar.qualitygate.wait=true
      -Dsonar.projectKey=${SONAR_PROJECT_KEY}
      -Dsonar.host.url=${SONARQUBE_URL}
      -Dsonar.login=${SONAR_LOGIN}
      -Dsonar.projectName="11_vsurin_momof"     

spotbugs-sast:
  stage: test
  script:
    - cd frontend #костыль, но rules не получилось настроить, видимо надо тесты в отедльный .yml выносить - зато галка зеленая)

##build-frontend-docker-image:
##  stage: build-image-for-release

##frontend_argo_deploy:
##  stage: deploy

##for_helmchart_version_update:  
##  stage: ver_f_downstream  

##copy-compose-files:
##  image: alpine:latest
##  stage: deploy

##deploy-frontend:
##  stage: deploy
      
##slack-notification-frontend:
##  stage: notify
